<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="http://fonts.googleapis.com/css?family=Open+Sans:300italic,400italic,600italic,700italic,800italic,400,300,600,700,800" rel="stylesheet" type="text/css">
    <title>México Urbano</title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body id="app">
    
    <div class="row">
    <div class="logo small-6 columns">
      <img src="<%= asset_path('logo.png') %>">
    </div>
    <div class="user small-6 columns text-right">
      Hola <%= current_user.email %>!<br/>
      <%= link_to "Editar Información", edit_user_registration_path %> | 
      <%= link_to "Salir", destroy_user_session_path, method: :delete %>
    </div>
    </div>
    
    <div class="row">
    <nav class="top-bar" data-topbar>
    <ul class="title-area">
      <li class="name">
        <h1></h1>
      </li>
      <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
    </ul>
     <section class="top-bar-section">
       <ul class="left">
         <li <%= is_active?("dashboard").html_safe %>><a href="#">Dashboard</a></li>
         <li <%= is_active?("advisors").html_safe %>><a href="#">Asesores</a></li>
         <li <%= is_active?("troopers").html_safe %>><a href="#">Brigadistas</a></li>
         <li <%= is_active?("kids").html_safe %>><a href="#">Niños</a></li>
         <li <%= is_active?("reports").html_safe %>><a href="#">Reportes</a></li>
         <li <%= is_active?("logs").html_safe %>><a href="#">Bitacoras</a></li>
       </ul>
     </section>
     </nav>
    </div>
    
    
    
    <% flash.each do |key, msg| %>
      <div class="row">
        <div class="small-6 small-centered columns">
          <div data-alert class="alert-box info radius">
            <%= content_tag :div, msg, id: key %>
            <a href="#" class="close">&times;</a>
          </div>
        </div>
      </div>
    <% end %>
    <div class="row content">
    <%= yield %>
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>
